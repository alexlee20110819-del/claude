node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# Lead spreadsheets stay local — never commit contact data.
*.xlsx
*.xls
*.csv
!src/**/*.csv

# Netlify CLI local state
.netlify

# Generated personal build data — real contact details, never commit.
src/data/bundled-leads.json
